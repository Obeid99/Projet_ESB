[supervisord]
nodaemon=true

[program:backend]
command=python -m flask run --host=0.0.0.0 --port=5000
directory=/app
autostart=true
autorestart=true

[program:frontend]
command=yarn start
directory=/app/frontend
autostart=true
autorestart=true